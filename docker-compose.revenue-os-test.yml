services:
  revenue-os-test-db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: revenue_os_test
      POSTGRES_USER: graxia
      POSTGRES_PASSWORD: graxia_test_only
    ports:
      - "55432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U graxia -d revenue_os_test"]
      interval: 5s
      timeout: 5s
      retries: 12

  revenue-os-test-redis:
    image: redis:7-alpine
    ports:
      - "56379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 12

