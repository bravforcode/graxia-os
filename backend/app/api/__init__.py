# Intentionally minimal — routers are imported directly by name so the
# Vercel serverless bundle stays slim. Importing every router here would
# pull the full stack (scrapers, ML, orchestrators) into every function.
