server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  - job_name: personal-os
    static_configs:
      - targets: [localhost]
        labels:
          job: personal-os
          __path__: /var/log/personal-os/**/*.log
