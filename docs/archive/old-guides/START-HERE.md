# ═══════════════════════════════════════════════════════════════════════════════
# 🚀 GRAXIA OS — START HERE (อันเดียวจบ!)
# ═══════════════════════════════════════════════════════════════════════════════

## ⚡ ใช้อันนี้อันเดียว! (One Script Only)

```powershell
# สตาร์ททั้งหมด (30+ services, 100 features)
.\graxia.ps1 up

# หรือถ้าไม่ได้
.\graxia.ps1                    # ดู help
```

---

## 🎯 คำสั่งทั้งหมด (รวมในไฟล์เดียว!)

| คำสั่ง | ทำอะไร |
|--------|--------|
| `.\graxia.ps1 up` | **สตาร์ททั้งหมด** - จัดการทุกอย่างอัตโนมัติ |
| `.\graxia.ps1 down` | **หยุดทั้งหมด** |
| `.\graxia.ps1 status` | **เช็คสถานะ** |
| `.\graxia.ps1 verify` | **เช็ค 100 features** |
| `.\graxia.ps1 logs api` | **ดู logs** |
| `.\graxia.ps1 shell api` | **เข้า container** |
| `.\graxia.ps1 fix` | **ซ่อมอัตโนมัติ** |
| `.\graxia.ps1 clean` | **ล้าง Docker** |

---

## 📦 ไฟล์เดียวจริงๆ!

**ใช้แค่ไฟล์นี้ไฟล์เดียว:**
- ✅ `graxia.ps1` — รวม CLI + Orchestrator + Sub-Agents + Configs

**ไม่ต้องเปิดอะไรเยอะอีกต่อไป!**

---

## 🎉 เริ่มใช้งาน

```powershell
# 1. สตาร์ท
.\graxia.ps1 up

# 2. เช็ค (รอสักพักแล้ว)
.\graxia.ps1 verify

# 3. เข้าใช้งาน
# http://localhost:8000        ← API
# http://localhost:3001        ← Grafana (admin/graxia_admin_2024)
```

---

**พร้อมใช้งานแล้ว! 🔥**
