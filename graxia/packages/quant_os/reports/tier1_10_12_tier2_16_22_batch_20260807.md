# Tier 1 #10-#12 / Tier 2 #16-#22 — Batch Execution Report (2026-08-07)

**Method:** 6 parallel research subagents (deep-research + subagent-driven-development + dispatching-parallel-agents) → implementer subagents for fixes. All claims evidence-based; no trust no hype.

## Tier 1

### #10 — N-value multiple-testing (FIXED, commit 28908bfc)
- Single source `validation/n_trials.py::get_reconciled_n_trials()` = 1050 confirmed, adopted at ALL DSR callers (14+ files: ws_a_trial_1028, run_tsmom_6sym_1033, run_fx_carry_3008, run_52week_high_1032, edge_search_m15_scalper, institutional_pipeline, exploratory_breadth_6sym, overfitting_detector, search_budget, strategy_validator, pipeline/runner, n_trials_i, check_candidate_dsr, edge_search_all).
- **SUSPECT found + fixed:** `run_rydc_validation.py` L827-830 hardcoded `n_trials=1001` (stale "cumulative trial count" comment; conflated RYDC trial #1001 with N). Replaced both occurrences with `get_reconciled_n_trials()`. No verdict impact (RYDC 1001 already REJECTED). Trial-ID 1001 references left intact.
- Other N values classified OK: 216 (edge_search_all Bonferroni, self-contained search, called out), 8 (tsm_ema per-search combos), 31 (pre_freeze diagnostics N-invariant), 1 (run_deflated_sharpe/direction_g per-trial DSR deliberate), 150/100/50/30 (Optuna tuning budgets), 13/100 (HoldoutValidator class defaults, no prod caller).
- **Gaps noted (not fixed):** test_n_trials_single_source.py only sweeps '1508' literal; legacy `run_dk_test` clones (edge_search_cross_sectional, edge_search_grid, run_ws_a, jackknife, pooled_trend, funding_rate_arb_rigor) have NO multiple-testing correction; reconciliation artifact doc L1454 still documents old dk_t>2.0 rule (doc-only drift).

### #11 — Sizing 100x units→lots (VERIFIED SINGLE_SOURCE)
- All 3 points use `core/contract_specs.py` as shared conversion: mt5.py L276 (units→lots converter, fail-closed), risk/engine.py `_layer4()` L539 (passes raw units via risk_based_units, no inline conversion), position_sizer_v2.py (no inline conversion; works in lots via broker contract size — but NOT wired into live path).
- **Out-of-scope findings (not fixed):** `api/webhook.py` L385-393 JPY `100000/100=1000` conversion WRONG (path KNOWN BROKEN); `execution/broker_adapter.py` L200 deprecated.

### #12 — MT5 _ensure_connected guards (FIXED, commit 28908bfc)
- WS-D previously patched entry guards (KNOWN_LIMITATIONS #8) but left 3 mid-retry reconnects unguarded.
- **Fixed (mirror submit_order L312-316):** cancel_order retry L410, close_position retry L581, set_stop_loss retry L679 (→ False), plus bonus get_price L630 (→ None) and update_trailing_stop L723 (→ False). All wrapped `try/except ConnectionError` → appropriate failure return.
- Verified: py_compile OK, ruff clean, no remaining unguarded calls in target methods. get_positions/get_account_info intentionally propagate (callers wrap; kill-switch safety validated — 8 active callers confirmed).

## Tier 2

### #16 — Master plan consolidation (NOTE, no action)
- Checklist is now the master TRACKER (header relationship note 2026-08-06). Other docs keep specific-scope roles. No further consolidation action.

### #17 — Single-writer generalization (DEFERRED — design task)
- Sentinel lock exists for Direction G Step 1 only. General/permanent mechanism needs design (out of scope for this batch — recommend a design spec).

### #18 — validation_stack false-pass (DEFERRED — needs user input)
- FAIL vs new UNKNOWN/ERRORED status design decision. Explicitly requires architectural/user input.

### #19 — Trial-ledger collision scan (FOUND + COORDINATION)
- **COLLISION_FOUND: 16 checker problems.** Root cause: **Direction J unregistered** — uses trial numbers 3003-3009 inside Direction B's owned 3001-3008 block. No `_j` row in TRIAL_ID_RANGES.md, no `_j` in checker FAMILY_RANGES (range check silently skips _j).
- Also: doc number mismatches (trial_3003_dxy_xauusd.md → main has #1003; trial_3010_fomc_drift.md → #1026), trial_1030 has no ledger entry despite TRIAL_ID_RANGES claiming occupied, multiple docs in wrong family folder.
- **F26 hazard confirmed:** hypothesis_registry_j.json modified DURING scan (12→15→16 problems across 3 runs). Writer lock held by `builder-dirj-3010` (stale PID at scan time but owner intent = renumber J → 3010 block).
- **DECISION (user):** do not seize — let the owning session renumber Direction J. Re-run checker after writes settle. Extra id-dup scan (same id → different number) = 0 collisions (clean).

### #20 — 18 stale worktrees (NOT addressed — pending)
- Still 18+ worktrees; cleanup pending (was not in this batch's fix list).

### #21 — Git stash triage (PARTIAL: @0 dropped)
- Actual count **21 stashes** (not 7/13/20 — earlier counts all wrong). @0 "wip-concurrent-before-review-rebase" = superseded (its `import types` addition already present at tree L123) → **dropped** (2352d15f). @3 vs @4 are NOT identical (controller correction wrong — @3 "P1 edits on wrong tree", @4 "WIP on fix/p1-backtest-engine"). Remaining stashes = other sessions' work — NOT touched.
- @9 = coderabbit config (NOT TRADING_MODE as checklist claimed).

### #22 — Post-commit vault-sync hook (FIXED — hook-side, no commit)
- Root cause: MSYS bash PID stored in lock (`$$`) recycles rapidly → stale lock's PID gets reassigned to unrelated live process → `kill -0` succeeds → sync skipped forever. No age check, no cleanup trap.
- **Fixed** `.git/hooks/post-commit` (backup .bak-20260807): lock format `<pid> <epoch>`, stale if PID dead OR lock >1800s. 10/10 functional tests. Live reproduction: killed-sync self-heals on next commit. Removed stale lock (PID 1165 dead).
- Status: STILL_PARTIAL→ FIXED (reliability now enforced by age+dead-PID detection).

## Commits
| Commit | Content |
|---|---|
| 28908bfc | #10 (rydc N) + #12 (MT5 mid-retry guards) — 2 files, +28/-7 |

## Not committed (outside repo control / deferred)
- #22: .git/hooks/post-commit fix (not a repo file)
- #21: stash drop (git ref, not a commit)
- #19: waiting on owning session (builder-dirj-3010)
- #16/#17/#18/#20: deferred as noted
