# Tier 1 #7/#8/#9 — Historical REJECT Trust — Closure Report (2026-08-06)

**Task:** Close Tier 1 items #7 (cost model 100x/slippage≡spread), #8 (FOREX_EDGE loader unknown), #9 (provenance backfill contamination) — the three "is our historical REJECT evidence trustworthy" questions.

## #7 — Cost model direction (CLOSED: bug did not exist / already fixed)

**Claims conflict resolved:**
- `deep_audit_v3/*` (DEPLOYMENT_READINESS L12, HONEST_SCORECARD L8, PRIORITIZED_NEXT_STEPS L18) cited `execution/cost_model.py:43` slippage≡spread + `engine.py:726` 100× FX error.
- `MEGA_PLAN_v3_ERRATA_20260722.md` Erratum 1 (L10-39) CLOSED both:
  - **1a:** cited file `backtest/slippage_model.py` has ZERO git history — hallucinated path. Actual model = `core/slippage_model.py` + `backtest/dynamic_spread_model.py` (created new in 05824e28, no rename).
  - **1b:** hand-calculated reference: EURUSD 1 lot → spread $10 + slip $3 + comm $7 = $20. Function output matches. "If a 100× error existed, total would be $2,000 or $0.20 instead of $20.00. It isn't."

**Verified today (2026-08-06):**
- `execution/cost_model.py` L40: `slippage_points: Decimal | None = None` — separate param; L49: `slip_pts = slippage_points if ... else spread_points * 0.3` — slippage NOT equal to spread.
- `backtest/engine.py` L726 = `get_overfitting_report()` (not `0.01 * spread_pips`). grep for `0.01.*spread_pips` / `slippage = spread_points` → zero matches.

**Verdict #7: CLOSED.** The deep_audit_v3 references were stale (pre-fix line numbers) or hallucinated (file path). Current cost model is correct (verified by errata's executable reference case + today's source read).

## #8 — FOREX_EDGE loader (CLOSED: runner + artifact located)

- Doc `FOREX_EDGE_INVESTIGATION.md` (2026-07-12) cited "Engine: canonical validation/walk_forward.py" — earlier audits (warehouse_dedup_exposure_audit_20260728 L40) could not locate the invoking script.
- **Runner FOUND:** `scripts/run_multi_instrument_wf.py` L36 `from validation.walk_forward import run_walk_forward` — same runner Direction H 9001 retest uses (stopping_rule_2026_08_06_direction_h.md L25-26).
- **Artifact FOUND:** `artifacts/wf_13_instruments/wf_batch_H1_20260712_190128.json` (timestamp 2026-07-12T12:01:28 — matches FOREX_EDGE date) + `artifacts/walk_forward/multi_instrument_oos.json`.
- Artifact parameters: train 500/test 200/step 200/min_confidence 0.65/seed 42; GBPUSD bars_total=50000.

**Verdict #8: CLOSED.** Runner = run_multi_instrument_wf.py; artifact exists; loader path = CSV/parquet.

## #9 — Provenance backfill contamination (CLOSED for FOREX_EDGE 6 trials)

- `run_multi_instrument_wf.py` `load_data()` (L59-94): tries CSV first (`{symbol}_{timeframe}.csv`), then parquet glob with `drop_duplicates()` (L92). Does NOT use the buggy warehouse path (the 6×-duplication bug fixed via `_dedupe_warehouse_ohlcv()`, F24).
- Warehouse dedup audit already confirmed Direction A/B + pooled trials load via CSV/parquet/DuckDB — now the FOREX_EDGE 6 trials are confirmed too (same runner's load_data).

**Verdict #9: CLOSED for these 6 trials.** Duplicated-observation inflation does not apply (CSV/parquet loader, not warehouse).

## Open note (not blocking)

- `wf_batch_H1_20260712_190128.json` records `total_cost: 0.0` for GBPUSD (and likely all 6) — the artifact was produced with cost=0, NOT the $7/rt commission FOREX_EDGE.md claims. Two possibilities: (a) artifact is a gross run (cost stripped), (b) cost was not applied in that run. Either way: the FOREX_EDGE.md "Quantified Cost Impact" section (L85-93, gross PnL +$23,714 - commission = net -$1.42) may be a post-hoc reconstruction rather than this artifact's direct output. The verdict direction (REJECT / INCONCLUSIVE) is unaffected — net PnL at cost=0 was already -$0.46 for GBPUSD — but the "$7/rt commission" narrative's provenance is not directly from this artifact. Flag for future re-run at measured costs (which Direction H 9001 already did at corrected costs: 9001 REJECTED_MIXED).

## Checklist updates applied

- #7: CLOSED (bug did not exist / fixed; deep_audit_v3 stale)
- #8: CLOSED (runner run_multi_instrument_wf.py + artifact located; loader CSV/parquet)
- #9: CLOSED for FOREX_EDGE 6 trials (no warehouse path); broader 1050-trial sweep remains open

---
END OF CONTENT
