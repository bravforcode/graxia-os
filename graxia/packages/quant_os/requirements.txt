# =============================================================================
# GRAXIA-OS v3.0 — Full Dependency List
# Auto-generated: 2026-06-27 | Python 3.11+ | Windows x64
# =============================================================================

# ---- Core (always required) ----
pandas>=2.0
numpy>=1.26.4
scipy>=1.11
pyarrow>=14.0
duckdb>=0.10
requests>=2.28
python-dotenv>=1.0
tomli>=2.0
PyYAML>=6.0

# ---- Broker / Live Execution (Windows-only, optional) ----
# MetaTrader5==5.0.5735  # Windows-only, install via: pip install quant_os[mt5]
ccxt>=4.0.0

# ---- Data Validation & Quality ----
pandera>=0.20

# ---- SMC / Order Flow Features ----
smartmoneyconcepts>=0.0.27
numba>=0.58
llvmlite>=0.41

# ---- Regime Detection ----
jumpmodels>=0.1
hmmlearn>=0.3

# ---- Cross-Validation & Overfitting ----
skfolio>=0.20
cvxpy-base>=1.4
clarabel>=0.6
qdldl>=0.1.7

# ---- Financial ML ----
fracdiff2>=1.0
statsmodels>=0.14
patsy>=0.5

# ---- Backtesting ----
vectorbt>=0.26
msgspec>=0.18
fsspec>=2023.1
portion>=2.4
dill>=0.3
regex>=2023.0
schedule>=1.2

# ---- Online ML / Drift Detection (may require numpy>=2.3) ----
# river>=0.22  # optional: install separately if needed

# ---- CFTC COT Reports ----
cot_reports>=0.1

# ---- ML / Stats ----
scikit-learn>=1.3
xgboost>=2.0
optuna>=3.4
joblib>=1.3

# ---- Visualization ----
matplotlib>=3.7
seaborn>=0.13
plotly>=5.18

# ---- Web / API ----
fastapi>=0.100
uvicorn>=0.23
httpx>=0.25

# ---- Telegram ----
python-telegram-bot>=20.0

# ---- Observability ----
structlog>=23.0
prometheus-client>=0.17

# ---- Config ----
pydantic>=2.0
pydantic-settings>=2.0

# ---- Testing ----
pytest>=7.0
pytest-asyncio>=0.21
hypothesis>=6.80

# ---- Linting ----
ruff>=0.4
mypy>=1.5
pre-commit>=3.0

# ---- New Modules (Phase 3.1+) ----
# ML Pipeline (model registry, feature store, drift monitor)
# Already included: xgboost, scikit-learn, pandas, numpy

# Risk Management (real-time risk, stress testing)
# Already included: scipy, numpy

# Execution (quality tracking, reconciliation)
# Already included: pydantic, structlog

# Monitoring (alerting, Grafana dashboard)
# Already included: structlog, httpx

# Deployment (blue-green, rollback)
# Already included: pydantic

# Strategy (ensemble, walk-forward)
# Already included: numpy, pandas, scikit-learn
