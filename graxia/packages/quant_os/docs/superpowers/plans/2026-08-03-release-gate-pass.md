# Plan: Release Gate Pass — Rolling Re-enable of Quarantined Tests

- **Date**: 2026-08-03
- **Status**: Draft (approved spec: `docs/superpowers/specs/2026-08-03-release-gate-pass-design.md`)
- **Owner**: builder agent
- **Related**: `quarantine_manifest.json` (35 entries QOS-VWAP-001, QOS-ORDERFLOW-001, QOS-MT5E2E-001, QOS-RB-001..032), `scripts/run_release_gate.py` (config 3639/3639, 35 `--ignore=` lines)

## Goal

`python scripts/run_release_gate.py` (from repo root `C:\Users\menum\graxia os`) exits **0 with all checks green**: all 35 quarantined files re-enabled in batches, config recalibrated to baseline reality, tree clean at run time, two reproducibility runs with equal stats and equal ledger seals.

## Architecture

- Repo root: `C:\Users\menum\graxia os`. All gate/pytest commands run from root; evidence-copy commands use workdir `graxia/packages/quant_os` (short relative paths avoid shell-quoting breakage).
- Gate internals (verified): `ARTIFACT_DIR` is FIXED at `graxia/packages/quant_os/artifacts/release_gate` (not label-parameterized) → copy evidence to `<label>/` subdirs after each run. `check_quarantine_consistency` compares pytest `skipped + ignored` vs manifest `total_quarantined` (read at line 176; manifest key at line 427). `parse_pytest_output` reverse-scans summary line. E2E run has 60s timeout.
- Manifest is source of truth; `--ignore=` lines in SUITE_CMD must always equal manifest entries.
- The 2 test files the other session already modified (`tests/test_autonomous_chaos.py`, `tests/test_orchestrator_reconciliation.py`) are their lane — never revert, never re-touch.

## Tech Stack

Python 3.x + pytest (run via `python -m pytest ... -q --tb=short`), git (conventional commits, pathspec commits to avoid sweeping the staged pair), PowerShell/cmd shell via bash tool (workdir param; no `Select-Object`; `Copy-Item` with short relative paths), native Edit/Write tools for file edits.

## Global Constraints

1. **Never modify the 35 quarantined test files** (other session's lane; except the 2 they already modified — leave untouched).
2. **Atomicity**: `run_release_gate.py` + `quarantine_manifest.json` committed in ONE commit, always.
3. **Config changed only after baseline measurement** (Task 2 from baseline; Task 7 from measured terminal state). Never speculative.
4. **Never commit other session's in-flight source files** (current set: `risk/engine.py`, `risk/stress_test.py`, `data/quality_gate.py`, `tests/conftest.py`, `validation/regime_analyzer.py`, `execution/adapters/base.py`, `paper_engine/analyzer.py`, `observability/opik_tracer.py`, `gold_bot/core/risk_bridge.py`, `.mcp.json`, `tests/chaos/test_core_untested.py`, + re-inventory live before every commit).
5. **Evidence**: every run's artifacts copied to `artifacts/release_gate/<label>/` (baseline, batch1..5, final).
6. All commands from repo root unless stated; ~20 min per full run; E2E 60s timeout built in.

## Interfaces (used by all tasks)

- Full suite: `python scripts/run_release_gate.py` (workdir `C:\Users\menum\graxia os`)
- Standalone verify: `python -m pytest graxia/packages/quant_os/tests/<file>.py -q --tb=short` (workdir root; bash-tool 120s default timeout kills hangs → file does NOT pass)
- Consistency one-liner (run from root; must print 3 EQUAL numbers):
  `python -c "import json; s=open(r'graxia/packages/quant_os/scripts/run_release_gate.py').read(); m=json.load(open(r'graxia/packages/quant_os/quarantine_manifest.json')); a=s.count('--ignore='); b=len(m['quarantined_tests']); c=m['total_quarantined']; print(a,b,c); assert a==b==c, 'INCONSISTENT'"`
- Evidence copy (workdir `graxia/packages/quant_os`): `Copy-Item artifacts\release_gate\* artifacts\release_gate\<label>\ -Recurse`

---

## Task 1 — Phase 0a: Clean-tree prep

### Files
- `graxia/packages/quant_os/.gitignore` (append churn patterns)
- Tracked churn to untrack: `graxia/packages/quant_os/tests/.test_tmp/list.json`, `graxia/packages/quant_os/data/visual_index/quant_meta.json`, `graxia/packages/quant_os/data/manifests/XAUUSD_D1.manifest.json` (regenerable runtime artifacts)
- Evidence to commit: `graxia/packages/quant_os/state/audit_log.jsonl`, `graxia/packages/quant_os/validation/.experiment_registry.json`
- Scratch to move: root `collect_order.txt commit1.txt commit2.txt pytest_collect_order.txt precommit_out3.txt precommit_out4.txt precommit_out5.txt precommit_out6.txt` + package `CLAUDE.md pytest_verify.txt sanity_post.txt`

### Steps
1. [ ] Re-inventory: `git status --porcelain` (root) → snapshot to `graxia/packages/quant_os/artifacts/release_gate/prep/initial_status.txt` (mkdir via workdir `graxia/packages/quant_os`).
2. [ ] Append to `graxia/packages/quant_os/.gitignore`:
   ```
   # === RELEASE GATE PASS 2026-08-03 ===
   # Runtime churn (regenerated by live runs / gate)
   data/heartbeat.txt
   data/kill_switch_state.json
   data/visual_index/quant_meta.json
   data/manifests/XAUUSD_D1.manifest.json
   tests/.test_tmp/
   ```
3. [ ] Untrack the 3 tracked churn files (files stay on disk):
   `git rm --cached graxia/packages/quant_os/tests/.test_tmp/list.json graxia/packages/quant_os/data/visual_index/quant_meta.json graxia/packages/quant_os/data/manifests/XAUUSD_D1.manifest.json`
4. [ ] Verify ignore rule matches: `git check-ignore -v graxia/packages/quant_os/data/heartbeat.txt` → prints a matching pattern line.
5. [ ] Move the 11 listed scratch files to `C:\Users\menum\AppData\Local\Temp\opencode\scratch-20260803\` (create dir first; use short relative paths from each file's own dir).
6. [ ] CRLF churn (conditional): if `scripts/check_bypass_loaders.py` or `scripts/validate_dtsmom_strategy.py` still modified → `git diff --numstat` on them; if 1/1 (whitespace-only) commit as-is. If they re-dirty after commit → add root `.gitattributes` with `* text=auto` and renormalize ONLY those 2 files (`git add --renormalize`), commit `.gitattributes`.
7. [ ] Evidence commit — pathspec only, must NOT sweep the staged pair:
   `git commit -m "chore(quant_os): commit runtime evidence (audit log, experiment registry)" -- graxia/packages/quant_os/state/audit_log.jsonl graxia/packages/quant_os/validation/.experiment_registry.json`
8. [ ] Post-check: `git status --porcelain` → expect `M  ...quarantine_manifest.json`, `MM ...run_release_gate.py` (pair still staged), churn gone, only other-session files dirty.

### Verify
- No churn files, no scratch untracked; evidence committed; staged pair intact.

---

## Task 2 — Phase 0b: Baseline run + config recalibration + atomic pair commit

### Files
- `graxia/packages/quant_os/scripts/run_release_gate.py` — config block (keep staged re-baseline + unstaged reverse-scan `parse_pytest_output` fix, already verified correct)
- `graxia/packages/quant_os/quarantine_manifest.json` (unchanged this task)
- `graxia/packages/quant_os/artifacts/release_gate/baseline/`

### Steps
1. [ ] Baseline run (expected RED — quarantine blocks pass; that is the point): `python scripts/run_release_gate.py` (~20 min).
2. [ ] Copy evidence to `artifacts/release_gate/baseline/`.
3. [ ] Extract counts from `artifacts/release_gate/pytest_output.txt` summary line: collected / passed / failed / errors / skipped.
4. [ ] Recalibrate: `required_passed_tests = 3639 − baseline_skipped` (if skipped > 0); keep `required_collected_tests = 3639` (terminal; Task 7 adjusts if legacy files stay ignored). If skipped = 0 → keep 3639. Record justification in commit body.
5. [ ] Stage the gate script (stages worktree incl. parse fix + config): `git add graxia/packages/quant_os/scripts/run_release_gate.py`
6. [ ] Atomic pair commit (commits only staged pair): `git commit -m "feat(quant_os): release gate config re-baseline (3639 minus N skips) + parse fix"`

### Verify
- `git log --oneline -3` shows pair commit; consistency one-liner prints 3 equal numbers; status shows only other-session files.

---

## Task 3 — Batch 1 (10): API/import drift + other-session-fixed files

### Files
- Gate SUITE_CMD + manifest — remove entries: QOS-RB-032, QOS-RB-004, QOS-RB-011, QOS-RB-003, QOS-RB-028, QOS-RB-030, QOS-RB-024, QOS-RB-021, QOS-RB-013, QOS-RB-001
- Re-enabled tests (paths relative to `graxia/packages/quant_os/`): `tests/test_tv_integration.py`, `tests/chaos/test_execution_untested.py`, `tests/test_config_unified.py`, `tests/chaos/test_deep_stress.py`, `tests/test_state_persistence.py`, `tests/test_synthetic_shock_scenarios.py`, `tests/test_quality_gate.py`, `tests/test_orchestrator_reconciliation.py`, `tests/test_e2e_signal_flow.py`, `tests/chaos/test_api_untested.py`
- `artifacts/release_gate/batch1/`

### Steps
1. [ ] `git status --porcelain` — confirm no NEW dirty files beyond the known other-session set. If the other session's source fixes for this batch are still uncommitted, poll ≤ 30 min; if blocked longer, pause and ask (E1).
2. [ ] Standalone verify each of the 10 files (any failure/hang → file stays quarantined this batch; record in `artifacts/release_gate/batch1/notes.md`).
3. [ ] Remove the green files' `--ignore=` lines from SUITE_CMD.
4. [ ] Remove the matching manifest entries; set `"total_quarantined"` to the new count.
5. [ ] Consistency one-liner → 3 equal numbers.
6. [ ] Atomic pair commit: `git add graxia/packages/quant_os/scripts/run_release_gate.py graxia/packages/quant_os/quarantine_manifest.json && git commit -m "test(quant_os): re-enable batch 1 (10 quarantined files)"`
7. [ ] Full suite `python scripts/run_release_gate.py` → GREEN: copy evidence to `batch1/`, next batch. RED: fix gate/import/API-drift issues in-batch only; re-run; if a file still fails in-suite → re-quarantine that file (reverse pair edit, move to next batch).

### Verify
- Suite green; consistency holds; `git log -1` = batch commit; notes.md written.

---

## Task 4 — Batch 2 (8): broker/risk/strategy behavior

### Files
- Remove entries: QOS-RB-002, QOS-RB-008, QOS-RB-005, QOS-RB-006, QOS-RB-009, QOS-RB-015, QOS-RB-010, QOS-RB-014
- Re-enabled tests: `tests/chaos/test_broker_adapters_unified.py`, `tests/test_autonomous_chaos.py`, `tests/chaos/test_full_pipeline.py`, `tests/chaos/test_strategies_untested.py`, `tests/test_backtest_refactor_b1_b3_c4.py`, `tests/test_lookahead_regression.py`, `tests/test_comprehensive.py`, `tests/test_ensemble_binarize_adapter.py`
- `artifacts/release_gate/batch2/`

### Steps
1. [ ] Same procedure as Task 3 (status check → standalone verify each → remove ignore+entry → consistency → atomic commit `test(quant_os): re-enable batch 2 (8 quarantined files)` → full suite → evidence).
2. [ ] `tests/test_autonomous_chaos.py` is other-session-modified: verify it passes standalone with THEIR worktree state; if it does, re-enable in this batch.

### Verify
- Suite green; consistency holds; evidence in `batch2/`.

---

## Task 5 — Batch 3 (8): data/quality/pipeline

### Files
- Remove entries: QOS-RB-012, QOS-RB-016, QOS-RB-017, QOS-RB-018, QOS-RB-019, QOS-RB-020, QOS-RB-025, QOS-RB-027
- Re-enabled tests: `tests/test_data_quality.py`, `tests/test_macro_data.py`, `tests/test_massive_sentiment.py`, `tests/test_monitoring_infra.py`, `tests/test_new_modules_benchmark.py`, `tests/test_new_modules_integration.py`, `tests/test_run_lagged_wf.py`, `tests/test_slippage_helper.py`
- `artifacts/release_gate/batch3/`

### Steps
1. [ ] Same procedure as Task 3 (commit: `test(quant_os): re-enable batch 3 (8 quarantined files)`).

### Verify
- Suite green; consistency holds; evidence in `batch3/`.

---

## Task 6 — Batch 4 (6): phase/validator/pyarrow

### Files
- Remove entries: QOS-RB-022, QOS-RB-023, QOS-RB-029, QOS-RB-026, QOS-RB-031, QOS-RB-007
- Re-enabled tests: `tests/test_phase_2a.py`, `tests/test_phase_be_p1.py`, `tests/test_strategy_validator_sanity.py`, `tests/test_safe_pickle.py`, `tests/test_trading_loop.py`, `tests/test_arrow_loader_c2.py`
- `artifacts/release_gate/batch4/`

### Steps
1. [ ] Same procedure as Task 3 (commit: `test(quant_os): re-enable batch 4 (6 quarantined files)`). Note: pyarrow-related entries (RB-031, RB-007) may require an environment check — standalone verify is the gate.

### Verify
- Suite green; consistency holds; evidence in `batch4/`.

---

## Task 7 — Batch 5 (3 legacy, E4): terminal quarantine decision + final config

### Files
- Gate SUITE_CMD + manifest — candidates: QOS-VWAP-001, QOS-ORDERFLOW-001, QOS-MT5E2E-001
- Tests: `tests/test_vwap.py`, `tests/e2e/test_order_flow.py`, `tests/test_mt5_live_order_e2e.py`
- `artifacts/release_gate/batch5/`

### Steps
1. [ ] Standalone verify each of the 3 (bash-tool 120s timeout kills hangs → not-pass).
2. [ ] Passed standalone → remove ignore + entry. Failed/hung → KEEP quarantine entry + ignore line (E4: environment-gated).
3. [ ] Consistency one-liner → 3 equal numbers.
4. [ ] Atomic pair commit: `test(quant_os): re-enable legacy batch 5 (N files), keep M environment-gated`
5. [ ] Full suite → measure TERMINAL counts: collected / passed / skipped / failed / errors.
6. [ ] Finalize config from measured terminal state only: `required_collected_tests = measured_collected`; `required_passed_tests = measured_collected − measured_skipped`; justification in commit body. Atomic pair commit: `feat(quant_os): finalize release gate config from terminal baseline`.

### Verify
- Consistency holds; terminal counts recorded in `batch5/notes.md`; config matches measured reality.

---

## Task 8 — Terminal state: reproducibility ×2 + evidence + report

### Files
- `artifacts/release_gate/final/`
- Evidence deltas: `state/audit_log.jsonl`, `validation/.experiment_registry.json`

### Steps
1. [ ] Commit evidence deltas (allowed evidence commit, pathspec).
2. [ ] `git status --porcelain` → only allowed state (no churn, no scratch, other-session files still untouched).
3. [ ] `python scripts/run_release_gate.py` run 1 → **GREEN**.
4. [ ] `python scripts/run_release_gate.py` run 2 (immediately after) → **GREEN**; compare `test_result.json` stats equal + e2e ledger seals equal.
5. [ ] Copy evidence → `artifacts/release_gate/final/`.
6. [ ] Write `artifacts/release_gate/final/SUMMARY.md`: batches, counts, config values + justification, seal hashes.
7. [ ] Final evidence commit; report to user.

### Verify
- Two green runs, equal stats, equal seals; `git status` clean (except evidence commit); SUMMARY.md present.

---

## Self-Review

- **Spec coverage**: §2 Phase 0 → Tasks 1–2; Phase 1..N → Tasks 3–7; Phase Final → Task 8. §4 verification plan → Task 2 step 1 (baseline before config), per-batch standalone verify + full-suite runs, final ×2 runs + seals, consistency re-verified every batch, clean status at final.
- **Edge cases mapped**: E1 → Task 3 step 1 (wait/poll/ask); E2 → Tasks 2+7 (baseline-only, justified commits); E3 → manifest as source of truth + consistency one-liner every batch; E4 → Task 7; E5 → Task 1 step 6; E6 → Global Constraint 4; E7 → accepted, batch order = pass likelihood; E8 → Task 1 step 7 + atomic pair commits throughout.
- **No placeholders**: every step has exact commands, paths, issue IDs, commit messages.
- **Right-sizing**: 8 tasks; each batch task is one sitting (~25–30 min incl. suite run); repetitive batch procedure defined once (Task 3) and referenced.
- **No-touch rule**: 33 test files untouched; 2 other-session files left as-is.

## Execute the plan. Which option do you want?

(a) Execute the plan inline in this session, checking in as I go
(b) Review the plan first, then execute
(c) Subagent-driven execution (spawn a builder subagent with this plan)
