{
  "framework": "vite",
  "installCommand": "cd frontend && bun install",
  "buildCommand": "cd frontend && bun run vite build",
  "outputDirectory": "frontend/dist",
  "functions": {
    "api/store_main.py": {
      "maxDuration": 10
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/store_main.py"
    },
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
